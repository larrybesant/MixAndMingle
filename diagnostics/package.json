{
  "name": "mix-mingle-diagnostics",
  "version": "1.0.0",
  "description": "Diagnostic tools for Mix & Mingle app",
  "main": "run-system-check.ts",
  "scripts": {
    "start": "ts-node run-system-check.ts",
    "fix-auth": "ts-node fix-auth-context-import.ts",
    "fix-vapid": "ts-node fix-vapid-key-issue.ts",
    "fix-all": "npm run fix-auth && npm run fix-vapid"
  },
  "dependencies": {
    "firebase-admin": "^11.10.1",
    "jsdom": "^22.1.0",
    "node-fetch": "^3.3.2",
    "redis": "^4.6.10",
    "twilio": "^4.19.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  },
  "devDependencies": {
    "@types/jsdom": "^21.1.3",
    "@types/node": "^20.8.10"
  }
}
