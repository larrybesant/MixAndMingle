specVersion: "v1"
serviceId: "mixandmingle"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "mixandmingle-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
